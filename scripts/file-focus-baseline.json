{
  "_why": "超过 300 行、但模块 JSDoc 里还没有「为什么不能再拆 / 待拆」的文件。基线化而不是让门禁常红：一个永远红的检查等于没有检查（同 verify:port 的做法）。**只能变小**：清掉一个就从这里删一个。**键是相对路径**（如 `src/agent.ts`），不是文件名 —— 现在 `src/` 与 `web/` 都会被扫到，`app.js` 这类名字跨目录重名时不会互相顶替。**2026-09-21 已清空**：agent.ts 补了说明，`web/` 也不再是盲区（两个文件当场被抓住并补上判断）。",
  "files": []
}
