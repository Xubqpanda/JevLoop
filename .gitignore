node_modules
package-lock.json
*.log
.run/
.DS_Store

# 密钥：绝不提交
.env
.env.local

# 构建产物：不进仓库（源码面 vs 产物面）
dist/

# 实验的数据与日志：不进仓库，见 experiments/README.md
#
# ★ 写法是 `dir/*` 加否定 `.gitkeep`，**不是**直接忽略整个目录 ——
#   直接写 `experiments/dataset/` 的话连占位文件也进不来，
#   于是别人 clone 出来那六个目录**根本不存在**，而 README 说它们存在。
#   ★ `dataset/` 里**进 git 的只有两份说明**:`.gitkeep`（把目录留住）和
#     `README.md`（约定）+ `DOWNLOADS.md`（**生成的**下载索引）。
#     **数据一个字节都不进** —— 但「怎么把它拿回来」必须写清楚,
#     否则别人 clone 下来只有一个空目录,这个仓库就不可复现了。
experiments/dataset/*
!experiments/dataset/.gitkeep
!experiments/dataset/README.md
!experiments/dataset/DOWNLOADS.md
experiments/log/*
!experiments/log/.gitkeep
#     ★ `README.md` 也要放行 —— **和上面 `dataset/` 同一个理由**。
#     跑出来的东西不进 git,但「这些目录是什么、哪些能引用」必须写清楚:
#     否则下一个人只会看到一堆旧目录,然后**挑一个引用**。
!experiments/log/README.md

# Python：字节码缓存不进仓库。
# ★ 不只是体积问题 —— .pyc 里**存着注释和 docstring 的原文**，
#   所以「源码里删干净了」和「仓库里没有」是两件事。
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# ★ 论文插图：**版权不属于我们，不进公开仓库。**
#   arXiv 的授权因文而异（有的 CC-BY 可以带署名转载，有的不是），
#   而这些图是**我们的读书材料**，不是代码的一部分。
#   放在本地 `baseline/<arm>/figures/` 里看就行。
experiments/baseline/*/figures/

# ★ 数据集二进制：**任何路径下都不进仓库。**
#   上一条按目录 ignore 是不够的 —— 路径算错一次（实测发生过：
#   缓存落到了 `experiments/benchmark/dataset/`），数据就进了暂存区。
#   按扩展名挡是**与路径无关**的那一道防线。
*.arrow
*.parquet
*.safetensors
*.bin
*.h5
*.idx
*.faiss
