node_modules
package-lock.json
*.log
.run/
.DS_Store

# 密钥：绝不提交
.env
.env.local

# 构建产物：不进仓库（源码面 vs 产物面）
dist/

# 实验的数据与日志：不进仓库，见 experiments/README.md §1
#
# ★ 写法是 `dir/*` 加否定，**不是**直接忽略整个目录 ——
#   直接写 `experiments/dataset/` 的话连占位文件也进不来，
#   于是别人 clone 出来那六个目录**根本不存在**，而 README 说它们存在。
#
# ★ `dataset/` 里进 git 的只有两份：`.gitkeep` 和 `MANIFEST.md`。
#   MANIFEST.md 是「怎么把数据拿回来」的索引（数据集 id / 版本 / 划分 /
#   Wikipedia dump 版本）—— **没有它，别人 clone 下来就是个空目录**。
experiments/dataset/*
!experiments/dataset/.gitkeep
!experiments/dataset/MANIFEST.md
experiments/log/*
!experiments/log/.gitkeep
